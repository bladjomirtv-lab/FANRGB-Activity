# RigWeave Activity

Public mobile dashboard for RigWeave orchestration and GitHub activity.

The repository slug remains `FANRGB-Activity` for URL/Page compatibility; the product-facing dashboard and PWA name are `RigWeave Activity`.
